{
  "name": "answerframe-functions",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "node ../node_modules/typescript/bin/tsc -p tsconfig.json",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "npm run build"
  },
  "dependencies": {
    "@answerframe/shared": "file:../packages/shared",
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
